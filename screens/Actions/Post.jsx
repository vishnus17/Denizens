import { View, Text } from "react-native";
import React from "react";
import { CreateFeed } from "../../components/Actions";
const Post = ({ navigation }) => {
  return <CreateFeed navigation={navigation} />;
};

export default Post;
